#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

float sensorMpu9250GyroConvert(int16_t data){
	return(data * 1.0) / (65536/500);
}

int main(){
	uint8_t value[10]= {0x64,0xff,0x02};
	int i;
	float result[3];
	float SCALE = 128.0;
		printf("%x",value[0]);
		printf("\n");
		printf("%d",value[1]);
                printf("\n");
		float gx = sensorMpu9250GyroConvert(value[0]);
		float gy = sensorMpu9250GyroConvert(value[1]); 
		float gz = sensorMpu9250GyroConvert(value[2]);
		printf("%f, %f, %f",gx,gy,gz);
		printf("\n");
	return 0;
}
